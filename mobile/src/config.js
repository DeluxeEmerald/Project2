// ---------------------------------------------------------------------
// EDIT THIS: point it at wherever your Express server (the one running
// your api.js routes) actually lives. This is NOT necessarily the same
// as your website's URL - it depends on how the backend is deployed:
//
//   - If your React website and this Express API are the SAME server
//     (Express serves both the built site and /api/* routes), use your
//     website's URL here.
//
//   - If they're deployed separately (e.g. website on Vercel/Netlify,
//     API on Render/Railway/Heroku/an EC2 box), use the API server's
//     own URL, not the website's.
//
//   - If you're just testing locally on your computer with the phone
//     app running in an emulator/Expo Go on the same Wi-Fi, use your
//     computer's LAN IP + port, e.g. 'http://192.168.1.50:5000'.
//     Do NOT use 'localhost' - the phone can't resolve that to your
//     computer.
// ---------------------------------------------------------------------
export const API_BASE_URL = 'http://cop4331-89.xyz:5000';
